using System;

namespace PJ
{
	public enum CoordinateType
	{
		Cartesian,	// On Y axis, + values are up
		Screen		// Origin: (0, 0) at top left of screen. X -> to the right. On Y axis, + values are down
	}
	
}

#include "ViewSizeProposal.h"

using namespace std;
using namespace PJ;

using This = ViewSizeProposal;

const This ViewSizeProposal::min{ .width = 0, .height = 0 };

/// Propose the ideal size
const This ViewSizeProposal::ideal{};
